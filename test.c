









#include <iostream>
using namespace std;
  int index = 0;
int main() 
{
    cout << "Hello, World!";
	int a, b, c;
	cout << "Enter two integers to add\n";
	cin >> a >> b;
	c = a + b; 
	int i = 0;
	for (i = 1; i <= a ++i) {
        c *= i;   // factorial = factorial * i;
    }

	if ( c > 100
    {
        cout << "Condition satisfied";
    } else {
		cout << "condition failed"
	}

    return 0;
}